---
import type { Author, Note, Link } from "../types/types";
import Links from "./Links.astro";
import Authors from "./Authors.astro";
import Notes from "./Notes.astro";
import Notes_big from "./Notes_big.astro";

interface Props {
  title: string;
  authors: Author[];
  institutions?: Note[];
  conference?: string;
  notes?: Note[];
  links: Link[];
}

const { title, authors, institutions, conference, notes, links } = Astro.props as Props;
---

<header class="flex flex-col gap-10 items-center mb-6">
  <h1>{title}</h1>
  <div class="flex flex-col gap-6 items-center">
    <Authors authors={authors} />
    {institutions && <Notes_big notes_big={institutions} />}
    {conference && <p class="text-center">{conference}</p>}
    {notes && <Notes notes={notes} />}
    {links && <Links links={links} />}
  </div>
</header>
